/**
 * The home pane is three elements that draw their own content, so this file's
 * only job is to register them.
 */
import "#components/site-footer";
import "#components/site-nav";
import "#components/tool-index";
